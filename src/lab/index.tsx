import EffectVisibleOnScrollViewPort from "./EffectVisibleOnViewPort";
import Grid from "./Grid";

const lab = [
  {
    name: "Effect on visible in the viewport (on scroll)",
    component: EffectVisibleOnScrollViewPort,
    route: "/effect-visible-on-viewport-scroll"
  },
  {
    name: "Grid / Sheet",
    component: Grid,
    route: "/grid"
  },
];

export default lab;
